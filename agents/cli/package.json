{
  "name": "agent-cli",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "test": "echo \"Error: no test specified for agents/cli. Run: 'npm -w agents/cli run dev' (Remember to start the servers from another terminal: 'snpm run dev')\" && exit 0",
    "start": "node dist/index.js",
    "dev": "node --loader ts-node/esm --no-warnings src/index.ts"
  },
  "dependencies": {
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.30"
  }
}